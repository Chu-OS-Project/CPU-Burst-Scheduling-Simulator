{
  "name": "cpu-scheduling-simulator",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "clean": "rimraf .next out",
    "build": "next build",
    "deploy": "npm run clean && next build && echo > out/.nojekyll && gh-pages -d out -t",
    "start": "next start",
    "lint": "eslint"
  },
  "dependencies": {
    "next": "16.0.3",
    "react": "19.2.0",
    "react-dom": "19.2.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "babel-plugin-react-compiler": "1.0.0",
    "eslint": "^9",
    "eslint-config-next": "16.0.3",
    "gh-pages": "^6.3.0",
    "rimraf": "^6.1.2",
    "tailwindcss": "^4"
  }
}
